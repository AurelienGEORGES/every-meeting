{
  "name": "every-meeting",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "container-api": "docker exec -it every-meeting-api bash", 
    "container-client": "docker exec -it every-meeting-client bash",
    "prepare": ".husky"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^8.0.0"
  }
}
